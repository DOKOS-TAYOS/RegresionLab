# Pip-only dependencies for Termux (numpy, scipy, pandas, matplotlib, Pillow from pkg)
# Streamlit omitted: requires pandas<3.0, which triggers rpds-py build (no ARM wheel). Use Tkinter GUI instead.
# pyarrow: NOT installed - no ARM wheel, build fails. Setup creates a stub so pandas skips it (ImportError).
openpyxl>=3.1,<4.0
python-dotenv>=1.0,<2.0
colorama>=0.4,<1.0
PyYAML>=6.0,<7.0
